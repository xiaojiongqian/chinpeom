# 测试环境数据库配置
# 注意：测试数据库名必须包含 "test" 字符串以确保安全

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=juphoon419708
DB_NAME=poem2ndguess
DB_CONNECTION_LIMIT=5

# JWT配置
JWT_SECRET=test-jwt-secret-key

# 环境配置
NODE_ENV=test

# 测试配置
TEST_TIMEOUT=15000

# 可选：测试数据保留设置
# KEEP_TEST_DATA=false  # 设置为 true 可在测试后保留数据用于调试 